package stream.answers;

import java.util.Arrays;
import java.util.function.BinaryOperator;

public enum Operator {
//    PLUS("+", Integer::sum),
//    MINUS("-", (number1, number2) -> number1 - number2),
//    MULTIPLE("*", (number1, number2) -> number1 * number2),
//    DIVIDE("/", (number1, number2) -> number1 / number2);
//
//    private final String signature;
//    private BinaryOperator<Integer> expression;
//
//    Operator(String signature, BinaryOperator<Integer>expression) {
//        this.signature = signature;
//        this.expression = expression;
//    }
//
//    public int calculate(int number1, int number2) {
//        return expression.apply(number1, number2);
//    }
//
//    public static Operator findOperator(String signature) {
//        return Arrays.stream(Operator.values())
//                .filter(t -> t.signature.equals(signature))
//                .findAny()
//                .orElseThrow(IllegalArgumentException::new);
//    }
}
